spring:
  cloud:
    config:
      name: config-server
      profile: dev
      label: master
      discovery:
        #您更喜欢使用DiscoveryClient以找到配置服务器，可以通过设置spring.cloud.config.discovery.enabled=true
        enabled: false
        serviceId: configserver
      uri: http://localhost:8888
